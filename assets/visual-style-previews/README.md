# 视觉模板预览资产

本目录的 SVG 卡片为本项目阶段 1 自制预览，仅用于在模板选择与审阅界面中快速辨认视觉方向；它们不是 provider 输出，也不作为正式 scene、质量批准或渲染验收证据。

这些预览没有复制 `cs-board` 的参考图片或其他来源不明的素材。正式成片仍固定使用 `#F5EBD7` 暖米黄画布和既有 `warm-paper-stream-v1` 流式渲染路径。

